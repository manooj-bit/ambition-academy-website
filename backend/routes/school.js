const express = require('express');\nconst School = require('../models/School');\nconst { verifyToken, requireAdmin } = require('../middleware/auth');\n\nconst router = express.Router();\n\n// Get school info\nrouter.get('/info', async (req, res) => {\n  try {\n    let school = await School.findOne();\n    if (!school) {\n      school = await School.create({});\n    }\n    res.json({\n      success: true,\n      school,\n    });\n  } catch (error) {\n    res.status(500).json({\n      success: false,\n      message: error.message,\n    });\n  }\n});\n\n// Update school info (Admin only)\nrouter.put('/info', verifyToken, requireAdmin, async (req, res) => {\n  try {\n    const {\n      name,\n      tagline,\n      location,\n      phone,\n      email,\n      facebook,\n      wallpaper,\n      about,\n      logo,\n      vision,\n      mission,\n    } = req.body;\n\n    let school = await School.findOne();\n    if (!school) {\n      school = new School();\n    }\n\n    if (name) school.name = name;\n    if (tagline) school.tagline = tagline;\n    if (location) school.location = location;\n    if (phone) school.phone = phone;\n    if (email) school.email = email;\n    if (facebook) school.facebook = facebook;\n    if (wallpaper) school.wallpaper = wallpaper;\n    if (about) school.about = about;\n    if (logo) school.logo = logo;\n    if (vision) school.vision = vision;\n    if (mission) school.mission = mission;\n\n    await school.save();\n\n    res.json({\n      success: true,\n      message: 'School info updated',\n      school,\n    });\n  } catch (error) {\n    res.status(500).json({\n      success: false,\n      message: error.message,\n    });\n  }\n});\n\nmodule.exports = router;\n