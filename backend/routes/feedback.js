const express = require('express');\nconst Feedback = require('../models/Feedback');\nconst { verifyToken, requireAdmin } = require('../middleware/auth');\n\nconst router = express.Router();\n\n// Get public feedback\nrouter.get('/', async (req, res) => {\n  try {\n    const feedback = await Feedback.find({ isPublic: true, approved: true }).sort({\n      createdAt: -1,\n    });\n\n    res.json({\n      success: true,\n      feedback,\n    });\n  } catch (error) {\n    res.status(500).json({\n      success: false,\n      message: error.message,\n    });\n  }\n});\n\n// Submit feedback\nrouter.post('/', async (req, res) => {\n  try {\n    const { name, email, userType, title, message, rating, isPublic } = req.body;\n\n    const feedback = new Feedback({\n      name,\n      email,\n      userType,\n      title,\n      message,\n      rating,\n      isPublic,\n    });\n\n    await feedback.save();\n\n    res.status(201).json({\n      success: true,\n      message: 'Feedback submitted',\n      feedback,\n    });\n  } catch (error) {\n    res.status(500).json({\n      success: false,\n      message: error.message,\n    });\n  }\n});\n\n// Get all feedback (Admin only)\nrouter.get('/admin/all', verifyToken, requireAdmin, async (req, res) => {\n  try {\n    const feedback = await Feedback.find().sort({ createdAt: -1 });\n\n    res.json({\n      success: true,\n      feedback,\n    });\n  } catch (error) {\n    res.status(500).json({\n      success: false,\n      message: error.message,\n    });\n  }\n});\n\n// Approve feedback (Admin only)\nrouter.put('/admin/:id/approve', verifyToken, requireAdmin, async (req, res) => {\n  try {\n    const feedback = await Feedback.findByIdAndUpdate(\n      req.params.id,\n      { approved: true },\n      { new: true }\n    );\n\n    res.json({\n      success: true,\n      message: 'Feedback approved',\n      feedback,\n    });\n  } catch (error) {\n    res.status(500).json({\n      success: false,\n      message: error.message,\n    });\n  }\n});\n\nmodule.exports = router;\n